import React from 'react';
import { Text, View } from 'react-native';

const Join = (params:any) => (

      <View style={{flex:1, justifyContent:"center", alignItems:'center'}}> 
        <Text> Join
        </Text>
      </View>
      
)

export default Join;